export interface LocationProps {
    as: string,
    city: string,
    country: string,
    countryCode: string
    isp: string,
    lat: number,
    lon: number,
    offset: number
    org: string,
    query: string // this is the IP
    region: string,
    regionName: string,
    status: string,
    timezone: string,
    zip: string
}